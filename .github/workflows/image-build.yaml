name: Container Image Build

on:
  schedule:
    - cron: "0 1 1 * *" # Run at 00:00 on the first day of the month
  push:
    branches:
      - "*"
    tags:
      - "v*.*.*"
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}
  PACKAGE_NAME: ${{ github.repository_owner }}/${{ vars.IMAGE_NAME }}

jobs:
  build-extra:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    continue-on-error: ${{ matrix.php_extensions.experimental }}
    strategy:
      max-parallel: 4
      matrix:
        php_extensions:
          - name: "base"
            experimental: false
          - name: "xdebug"
            experimental: true
          - name: "pcov"
            experimental: true
          - name: "oci"
            experimental: true
          - name: "oci-xdebug"
            experimental: true
          - name: "oci-pcov"
            experimental: true

    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Setup QEMU
        uses: docker/setup-qemu-action@v3
      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Get & Set Container Metadata
        id: meta-packages
        uses: docker/metadata-action@v6
        with:
          images: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            ${{ matrix.php_extensions.name }}
            type=raw,value=latest,enable=${{ matrix.php_extensions.name == 'base' }}

      - name: Build & Push
        id: build-and-push
        uses: docker/build-push-action@v6
        with:
          context: ./docker-extra
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta-packages.outputs.tags }}
          labels: ${{ steps.meta-packages.outputs.labels }}
          target: ${{ matrix.php_extensions.name }}
          build-args: |
            BASEIMAGE=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:base
          cache-from: type=registry,ref=ghcr.io/${{ github.repository }}:buildcache
          cache-to: type=registry,ref=ghcr.io/${{ github.repository }}:buildcache,mode=max
